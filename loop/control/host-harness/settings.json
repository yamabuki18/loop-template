{
  "//": "OPTIONAL host-side guard. Use ONLY if you keep an interactive supervisor Claude open in the project root. Install: ./control/scaffold.sh --install-host-guard  (copies the hook to a bin dir and merges these hooks into .claude/settings.json). The autonomous loop does NOT need this — it runs no host Claude. See host-harness/README.md.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [{ "type": "command", "command": "$CLAUDE_PROJECT_DIR/control/host-harness/harness-guard-secrets" }]
      },
      {
        "matcher": "Read|Edit|Write|MultiEdit|NotebookEdit",
        "hooks": [{ "type": "command", "command": "$CLAUDE_PROJECT_DIR/control/host-harness/harness-guard-secrets" }]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "ExitPlanMode",
        "hooks": [{ "type": "command", "command": "$CLAUDE_PROJECT_DIR/control/host-harness/harness-plan-capture" }]
      }
    ]
  }
}
