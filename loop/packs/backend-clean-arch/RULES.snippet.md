## バックエンド設計規約（backend-clean-arch パック）
- コア（CORE_DIRS 配下）にフレームワーク/インフラの import を追加しない。必要になったら
  ポート（インターフェース）を切ってアダプタ側に実装する。
- 新しいユースケースはコアに単体テストを併設する（外部依存ゼロで走ること）。
- この規則は L2 ガード（編集時）と gate（マージ時）が機械検査する。回避せず、境界が誤って
  いると考えるなら STATUS に理由を書いて監督に委ねること。
